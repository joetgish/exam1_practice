/*
Practice Program 2

Write a program which implements and tests the following requirements:

Write and test the addUp(int valA) function which calculates and returns the sum of the integers from 1 to valA (inclusive). If a negative number is passed to the function, the function should return -999. At a minimum, the following tests must be used and passed:
	
	assert(addUp(-4) == -999);
	assert(addUp(1) == 1);
	assert(addUp(50) == ?? );

___________________________________________________________________
*/


#include <iostream>
using namespace std;

int main()
{



	return 0;
}
