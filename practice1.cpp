// Practice Program 1
//
// Write a program which implements and tests the following requirements:

// Write and test the isPassed(double valA, double valB) function which averages the two inputs and 
// returns true if the average is between 80 to 100 (inclusive) and returns false if the function is less than 80 
// or more than 100. At a minimum, the following tests must be used and passed [remember 0 is false and 1 
// is true];

// 	assert(isPassed(85.0, 78.0) == 1);
// 	assert(isPassed(180.0, 100.2) == 0);
// 	assert(isPassed(-90, 68.0) == 0);
//  *************************************************************************

#include <iostream>
using namespace std;

int main()
{



	return 0;
}
